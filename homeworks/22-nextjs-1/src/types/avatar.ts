/**
 * Represents an avatar entity with a unique identifier and image URL
 * @typedef {Object} Avatar
 * @property {number} id - Unique identifier for the avatar
 * @property {string} url - URL pointing to the avatar image
 */
export interface Avatar {
  id: number;
  url: string;
}
